from typing import Any, Dict


def gemini_json(model: str, prompt: str, temperature: float = 0.2) -> Dict[str, Any]:
    """
    Placeholder.
    When enabled, install: google-genai (Google Gen AI SDK) and set GEMINI_API_KEY.

    We'll implement once you confirm the exact Gemini setup you want.
    """
    raise NotImplementedError(
        "Gemini client not enabled yet. "
        "Install Google GenAI SDK and set GEMINI_API_KEY, then implement gemini_json()."
    )